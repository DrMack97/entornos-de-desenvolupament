sequenceDiagram
    autonumber
    participant A as Actor (Tutor/Médico)
    participant S as Servidor (API)
    participant B as Base de Datos

    rect rgba(240, 248, 255, 0.7)
        Note over A, B: Caso de Uso: Ver Niño (View Child)
        A->>+S: GET /api/childs/{id} (Token)
        S->>+B: Validar permisos y recuperar datos
        B-->>-S: Datos del niño + Tratamiento
        S-->>-A: 200 OK (JSON con perfil y estadísticas)
    end